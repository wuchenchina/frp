version: '3.9'
services:
    frp:
        image: 'wuchenchina/frp:1.0.0'
        volumes:
            - './frps.ini:/data/ftps.ini'
        network_mode: "host"
        tty: true
        stdin_open: true
        restart: "no"
